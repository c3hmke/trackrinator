import 'package:flutter/material.dart';

class WorkoutB extends StatelessWidget
{
  const WorkoutB({super.key});

    @override
    Widget build(BuildContext context)
    {
        return Center(
            child: Column(
                mainAxisAlignment: MainAxisAlignment.center,
                children: <Widget>
                [
                    Text('foo'),
                ],
            ),
        );
    }
}